<div class="aa-cta">{% render 'aa-cta-button', ctakey: primary, variant: 'primary', product: product, show_cart: show_cart %}</div>
{%- if secondary -%}<div style="margin-top:12px">{% render 'aa-cta-button', ctakey: secondary, variant: 'secondary', product: product, show_cart: false %}</div>{%- endif -%}
